# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env.production

# TanStack Router local cache (router tree generator temp files)
.tanstack/

# Playwright run output. Failure snapshots can include filled password
# fields — never commit them.
test-results/
playwright-report/
playwright-report-prod-tenant/
e2e/prod-tenant/_config/playwright-report-prod-tenant/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
src/paraglide/

# pen.dev (design/) — the .pen sources ARE committed; the renders are not.
design/exports/
design/**/*.png
design/**/*.jpg
design/**/*.jpeg
design/**/*.webp
design/**/*.pdf
design/**/*.html
