export function buildHexIndex(cssPath?: string): Map<string, string[]>

export function contrastRatio(firstHex: string, secondHex: string): number

export function suggestionFor(tokenNames: string[]): string
