# Per-service Trivy ignore-list for whisper-server.
# Schema: SPEC-CI-TRIVY-POLICY-001 REQ-5 (id + ≥40-char statement + expired_at).
# Referenced from .github/workflows/whisper-server.yml via `trivyignores:`.

# 2026-09-25: CVE-2025-68973 (gnupg/gpgv) entry removed. The re-evaluation
# criterion from the previous entry — Ubuntu 24.04 shipping gpgv >=
# 2.4.4-2ubuntu17.4 — is met: the Dockerfile's `apt-get upgrade -y` step
# (already present, runs before the gnupg purge) now resolves gpgv to
# 2.4.4-2ubuntu17.6 from noble-security. Reproduced the Dockerfile's
# update/upgrade/install/purge sequence against
# nvidia/cuda:12.9.1-cudnn-runtime-ubuntu24.04 and confirmed via
# `dpkg -l gpgv` (2.4.4-2ubuntu17.6) and a `trivy image --ignore-unfixed`
# scan of the resulting image (0 findings).
vulnerabilities: []
