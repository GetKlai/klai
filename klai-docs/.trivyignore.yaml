# Per-service Trivy ignore-list for klai-docs.
# Schema: SPEC-CI-TRIVY-POLICY-001 REQ-5 (id + ≥40-char statement + expired_at).
# Referenced from .github/workflows/docs.yml via `trivyignores:`.

# 2026-09-25: CVE-2026-33671 (picomatch) entry removed. `trivy fs` against
# klai-docs/package-lock.json with a freshly downloaded vulnerability DB
# reports 0 findings for picomatch 2.3.2 / 4.0.5 / 4.0.7 (the versions
# currently resolved) — the Trivy DB lag noted at the previous renewal has
# resolved and the database now recognises 4.0.4+ as fixed. No entry is
# needed until a new finding surfaces.
vulnerabilities: []
