# Baseline mode — no CodeIndex, pure mini-swe-agent (control group)
agent:
  agent_class: "eval.agents.codeindex_agent.CodeIndexAgent"
  codeindex_mode: "baseline"
  step_limit: 30
  cost_limit: 3.0

environment:
  environment_class: "docker"
