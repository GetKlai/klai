# Sync Report — SPEC-PORTAL-PRICING-PER-USER-001 v0.5.x

**Generated:** 2026-05-13T06:37:31Z
**SPEC status:** `shipped` (v0.5.1)
**Sync mode:** auto (post-merge documentation sync)
**Base commit (pre-sync):** `bbba9e0e` (PR #624 merge)

---

## What was synced

This sync runs **after** PR #621 (v0.5.0 viewer-drop + account-type-derived-from-Profile + UX rename) and PR #624 (v0.5.1 polish) had already been merged and deployed. The work was complete; this sync covers documentation and audit-trail completion only.

### Documents updated

| File | Change |
|---|---|
| `.moai/specs/SPEC-PORTAL-PRICING-PER-USER-001/spec.md` | Added v0.5.1 HISTORY row; frontmatter version bumped 0.5.0 → 0.5.1 |
| `.moai/project/product.md` | "Product Entitlements & Plans" section rewritten to describe per-user account-type as billing source-of-truth; org plan demoted to quota/feature-ceiling helper |
| `CHANGELOG.md` | New top-of-file `[Unreleased] — 2026-05-13` entry covering the full v0.5.x arc (Added / Changed / Fixed / Architecture / Verification / Coverage / PRs) |

### Documents NOT updated (and why)

| File | Reason |
|---|---|
| `.moai/project/structure.md` | No directory/module structure changes from v0.5.x (only existing files touched) |
| `.moai/project/tech.md` | No new dependencies/technologies added |
| `.moai/project/codemaps/` | No architecture-graph changes (orthogonal `seat_type`+`role` columns survived intact) |
| `README.md` | Top-level README does not describe pricing — no relevant section to update |
| Code (anywhere) | All code shipped via #621/#624 already on prod; this sync is doc-only |

---

## Pre-existing changes in working tree (NOT touched by this sync)

The canonical repo had pre-existing unstaged changes that predate this session and are unrelated to SPEC-PORTAL-PRICING-PER-USER-001. Per `minimal-changes` rule, these are explicitly NOT committed:

- `klai-infra` (submodule pointer)
- `klai-website` (submodule pointer)
- `CLAUDE.md`, `AGENTS.md`, `.serena/project.yml`, `.claude/skills/codeindex/codeindex-cli/SKILL.md`
- `.claude/scheduled_tasks.lock`
- Numerous `.png` screenshot artifacts (`klai-mobile-*.png`, `klai-live-*.png`, `e2e-*.png`, `add-connector-*.png`, etc.)

These belong to other work-in-progress; the sync commit MUST leave them untouched.

---

## Divergence analysis (Phase 1.5)

**Lifecycle level:** Level 2 (spec-anchored — SPEC content is updated to reflect actual implementation; SPEC document is the durable record).

**Implementation vs SPEC:**
- v0.4.0 body still documents the 3-tier (`viewer / chat / knowledge`) architecture — this is the as-shipped-at-Phase-1-6 state.
- v0.5.0 HISTORY row documents the pivot to 2-tier + Profile-derived invite UI.
- v0.5.1 HISTORY row documents the polish.
- AS-OF v0.5.0 callout table near the top of the body maps body-vs-live divergences for readers scanning the SPEC.

**No remaining drift.** Code, docs, and SPEC are aligned at v0.5.1.

---

## Pre-flight verifications (already validated by CI on #621 + #624 merges)

These were verified during the PR cycle; the sync does not re-run them but records the state:

- Backend pytest: **2630/2630** passing (was 2619 pre-v0.5.x; +11 migration regex tests)
- Frontend vitest: **238/238** passing (was 225 pre-v0.5.x; +13 derived-badge + a11y assertions)
- `ruff check` + `ruff format --check` + `pyright`: clean
- `tsc -b --noEmit` + `eslint`: clean
- i18n parity: en.json ⇄ nl.json symmetric, 1761 keys each
- Alembic: single head `f1ff304b7b0a`
- Live prod (Voys tenant, Google SSO): account-type badge derives correctly; A11y attributes present; no Viewer mentions; migration applied; CHECK constraint narrowed.

---

## Open follow-ups

- **SPEC-PORTAL-MONEYBIRD-PER-SEAT-001** v0.2.0 status `needs-research` — Q1-Q5 still open on Moneybird subscription API behaviour, mandate portability, cancel-and-replace gap-day. Blocks Phase 5b implementation until research lands.
- **Body of parent SPEC** still documents v0.4.0 three-tier narrative in 1500+ lines. Bewuste keuze (Mark: "geschiedenis niet verliezen"); the "AS-OF v0.5.0" callout near the top is the canonical body-vs-live diff. If a future SPEC update wants to collapse this, move v0.4.0 body to `archive/v0.4.0-body.md` and rewrite the body for v0.5.x.
- **Session-boundary git tag** — added by this sync (`moai/SPEC-PORTAL-PRICING-PER-USER-001/sync-complete`) pointing at the canonical post-PR-#624 merge commit.

---

## Reproducibility

This sync was deterministic. Re-running it (`/moai sync SPEC-PORTAL-PRICING-PER-USER-001`) on the same git SHA would produce an idempotent no-op: HISTORY row, product.md section, CHANGELOG entry all already at v0.5.1 / shipped state.
