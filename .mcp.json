{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/.mcp.schema.json",
  "mcpServers": {
    "serena": {
      "$comment": "Semantic code navigation and persistent project memories. Uses installed binary — NOT uvx (too slow, exceeds MCP timeout).",
      "type": "stdio",
      "command": "serena",
      "args": ["start-mcp-server", "--project-from-cwd"],
      "env": {}
    },
    "context7": {
      "$comment": "Up-to-date documentation and code examples via Context7",
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "env": {}
    },
    "playwright": {
      "$comment": "Browser automation for E2E spot-checks. Uses Brave with persistent profile (~/.claude/mcp-brave-profile) for login state. MUST close browser after testing to release profile lock.",
      "type": "stdio",
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--browser", "chromium", "--executable-path", "/Applications/Brave Browser.app/Contents/MacOS/Brave Browser"],
      "env": {}
    }
  }
}
