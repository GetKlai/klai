{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/.mcp.schema.json",
  "mcpServers": {
    "serena": {
      "$comment": "Semantic code navigation and persistent project memories. Uses installed binary — NOT uvx (too slow, exceeds MCP timeout).",
      "type": "stdio",
      "command": "serena",
      "args": ["start-mcp-server", "--project-from-cwd"],
      "env": {}
    },
    "context7": {
      "$comment": "Up-to-date documentation and code examples via Context7",
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "env": {}
    },
    "playwright": {
      "$comment": "Browser automation for E2E spot-checks. Uses persistent Brave profile. Stop hook auto-enforces browser_close. Supports localhost (dev) and *.getklai.com (prod).",
      "type": "stdio",
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--config", ".playwright-mcp/config.json"],
      "env": {}
    },
    "grafana": {
      "$comment": "AI agent access to Grafana dashboards, VictoriaLogs queries, and alerts. Read-only mode. Requires GRAFANA_SERVICE_ACCOUNT_TOKEN (create in Grafana → Admin → Service Accounts).",
      "type": "stdio",
      "command": "uvx",
      "args": ["mcp-grafana", "--disable-write"],
      "env": {
        "GRAFANA_URL": "https://grafana.getklai.com",
        "GRAFANA_SERVICE_ACCOUNT_TOKEN": ""
      }
    }
  }
}
