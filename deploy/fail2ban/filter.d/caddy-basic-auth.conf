[Definition]
# Match failed basic auth attempts in Caddy JSON access logs
# Bans IPs that repeatedly get 401 on grafana.${DOMAIN}
failregex = .*"client_ip":"<HOST>".*"host":"grafana\.[^"]*".*"status":401

ignoreregex =
