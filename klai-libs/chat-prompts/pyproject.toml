[project]
name = "klai-chat-prompts"
version = "0.1.0"
description = "Shared chat system-prompt constants for Klai services (SPEC-RAG-MULTILINGUAL-CHAT-001)."
requires-python = ">=3.12"
readme = "README.md"
license = { text = "Proprietary" }
authors = [{ name = "Klai" }]

# langid powers klai_chat_prompts.language (conversation-level language
# decisions): BSD, pure Python, model embedded in the package, needs numpy.
# The LiteLLM container cannot pip-install, so it carries a vendored copy of
# langid next to the flat module instead (see NOTICES.md). All other consumers
# (portal-backend, retrieval-api) get langid through this path-dependency.
# pydantic backs the answer-claims classification schema (SPEC-RAG-CLARIFY-FLOW-001
# REQ-1b); klai-citations supplies the salient-token matcher has_direct_evidence_for_query
# needs (REQ-1c) — both already transitive deps of every consumer of this library.
dependencies = ["langid>=1.1.6", "pydantic>=2.9", "klai-citations"]

[tool.uv.sources]
klai-citations = { path = "../citations", editable = true }

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "ruff>=0.8",
    "pyright>=1.1.390",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "ruff>=0.8",
    "pyright>=1.1.390",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["klai_chat_prompts"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "S", "RUF"]
ignore = [
    "S101",  # assert is fine in tests
]

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "strict"
